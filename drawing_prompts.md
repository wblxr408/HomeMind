# HomeMind 绘图提示词

本文件用于生成 `main.tex` 报告中的主视觉、系统总览图、架构图和产品界面示意图。当前版本与报告中的端云协同、安全澄清、低代码生成和空间优先设备面板设计保持一致。

## 统一视觉规范

- 风格：现代科技产品报告插图，清晰、克制、工程化，适合学术竞赛项目报告。
- 主色：绿色 `#1D9E75`，深绿色 `#167A5A`，浅绿色 `#E1F5EE`，蓝色 `#2E86AB`，浅灰 `#F8F9FA`。
- 字体观感：中文无衬线，英文技术标签可使用等宽或清晰无衬线。
- 画面比例：优先2：1插图比例，便于插入 LaTeX。
- 禁止：不要做营销海报风，不要过度渐变，不要复杂背景，不要让箭头和文字重叠。

## 图 1：HomeMind 端侧智能体总体方案概览

```text
请绘制一张 HomeMind 端侧智能家居智能体系统总览图。

画面中心是一条端侧 Agent 主链路，从左到右展示：
用户文本/语音输入 -> 语言归一化 -> BSR 广召回 -> LSR 轻量精排 -> Router 路由 -> LLM 结构化决策 -> 设备/场景/TAP 执行 -> 反馈写回。

在主链路下方展示本地记忆层：
SessionStore 会话上下文、PreferenceStore 结构化偏好、KnowledgeBase/RAG 长期记忆。
记忆层用虚线箭头回注到 BSR、LSR 和 LLM。

在主链路右上方展示云端大模型协同模块，必须明确区分两条路径：
1. 常规控制类：设备控制、场景切换、信息查询，先经过本地 BSR/LSR，再由 Router 按置信度决定是否上云。
2. 低代码生成类：场景新建、TAP 自动化规则生成、多轮 Change 纠正，云端可以前置生成结构化草案，然后回到端侧做设备、空间、协议、Schema 和安全校验。

在执行层前方增加红色或橙色的安全澄清门控，标注：
门锁、门禁、摄像头布防/撤防、安防报警等安全敏感动作，无论置信度高低都必须先澄清确认。

右侧展示家庭空间和设备图标：客厅、主卧、书房、厨房、玄关等空间，每个空间包含灯、空调、传感器、门锁等设备。强调“空间 + 设备 + 协议”共同校验。

整体风格应是清晰的系统架构插图，白底，绿色主链路，蓝色云端协同，橙色安全门控，灰色本地存储层。
```

## 图 2：端侧智能体七层架构图

```text
请绘制一张分层系统架构图，标题为“HomeMind 端侧智能体总体架构”。

主区域从上到下分为七层：
1. 交互层：Web 前端、文本输入、Vosk 离线 ASR、口语/方言归一化。
2. BSR 理解层：规则召回、向量召回、历史召回，三路并行后汇聚 Top-K。
3. LSR 理解层：5 维环境与偏好特征，轻量精排，输出 Top-1/Top-3。
4. Router 决策层：本地直达、云端协同、澄清询问、fallback。
5. LLM 决策层：Mock、llama.cpp、OpenAI 兼容 API，固定 JSON 输出。
6. 执行层：DeviceController、SceneSwitcher、TAP 引擎、协议网关。
7. 记忆层：RAG 知识库、偏好库、会话状态、本地 JSON/ChromaDB。

右侧添加“云端协同”虚线框：
云端模块包含“模糊裁决”和“草案生成”两项能力。
从 Router 到云端画一条虚线箭头，标注“控制类：BSR/LSR 后按置信度上云”。
从云端返回 LLM/校验层画一条虚线箭头，标注“草案/裁决回注本地校验”。
另画一条从低代码任务入口到云端的前置箭头，标注“低代码：云端先生成草案”。

在 Router 和执行层之间增加安全门控：
“安全敏感动作强制澄清：门锁 / 门禁 / 安防布撤防 / 摄像头”。

在记忆层向 BSR、LSR、LLM 画虚线回注箭头，标注“历史偏好检索、RAG 上下文注入、反馈写回”。

画面应适合放进论文或项目报告，节点简洁，箭头方向清晰。
```

## 图 3：端云协同决策流程图

```text
请绘制一张 HomeMind 端云协同决策流程图，突出“云什么时候在前，什么时候在后”。

从用户输入开始，先进入任务类型识别，分成三条分支：

分支 A：常规控制任务
包括设备控制、场景切换、信息查询。
流程为：本地语言归一化 -> BSR 召回 -> LSR 重排 -> Router 置信度判断。
高置信度：本地直达。
中置信度/复杂语义：压缩最小上下文后上云裁决。
低置信度：澄清询问。
云端结果返回后必须经过 Schema 校验、设备校验、空间校验和协议校验，才能进入执行层。

分支 B：低代码生成任务
包括场景新建、TAP 自动化规则生成、多轮 Change 纠正。
流程为：用户自然语言描述 -> 云端生成结构化草案 -> 本地设备/空间/协议/Schema/安全校验 -> UI 可编辑 -> Accept/Change/Reject -> 保存或进入下一轮修改。

分支 C：安全敏感任务
包括门锁、门禁、摄像头布防/撤防、安防报警开关。
流程为：识别到安全敏感动作 -> 无论本地或云端置信度多高 -> 必须澄清确认目标空间、目标设备、动作意图 -> 用户明确确认后才进入本地校验；确认不足则拒绝执行或继续澄清。

使用绿色表示端侧本地链路，蓝色表示云端大模型，橙色表示澄清/安全门控，灰色表示校验与 UI 编辑。
```

## 图 4：关键创新与技术特征图

```text
请绘制一张“HomeMind 关键创新与技术特征”信息图，适合放在项目报告“关键创新与技术特征”小节。

画面中心放置 HomeMind 端侧智能体核心，周围以 6 到 8 个模块环绕展示创新点，每个模块用简洁图标、短标题和一句说明表达。

必须包含以下创新点：
1. 端侧优先的轻量智能体闭环：
   BSR -> LSR -> Router -> LLM -> Execution -> Memory，强调高频任务留在本地。
2. 面向家庭成员差异的语言适配：
   Vosk 离线 ASR、中文/英文、口语和方言归一化，例如“闷得慌”“困告了”。
3. 基于上下文记忆的轻量理解：
   会话上下文、结构化偏好、RAG 长期记忆回注到召回、重排和决策。
4. 任务类型驱动的端云协同：
   常规控制先本地 BSR/LSR 后按置信度上云；低代码场景/规则/Change 任务允许云端先生成草案。
5. 原子化短记忆设计：
   用户纠正、接受动作、长期习惯以短经验条目存储，便于检索和写回。
6. 上下文感知轻量重排序：
   BSR 得分、温度、湿度、时间、偏好 5 维特征驱动 LSR。
7. 结构化可控执行链路：
   固定 JSON、Schema 校验、设备/空间/协议校验、执行反馈。
8. 家庭安全强制澄清：
   门锁、门禁、安防布撤防、摄像头等动作无论置信度都先澄清确认。

视觉结构建议：
中心是 HomeMind，外围用放射状或矩阵卡片展示创新点。
绿色表示端侧主链路，蓝色表示云端协同，橙色表示安全澄清，灰色表示本地记忆与校验。
整体不要像营销海报，要像技术报告中的创新点总览图。
```

## 图 5：系统用例图

```text
请绘制一张 HomeMind 系统用例图，使用 UML 用例图或清晰的产品用例示意风格。

参与者：
1. 普通家庭用户：通过自然语言或语音使用系统。
2. 家庭管理员：管理设备、空间、户型图、协议、规则和场景。
3. 云端大模型服务：提供模糊语义裁决和低代码草案生成。
4. 智能家居设备/协议网关：Matter、MQTT、米家、Home Assistant 等真实设备接入。

系统边界命名为 HomeMind 端侧智能家居 Agent。

普通家庭用户用例：
- 自然语言设备控制
- 场景切换
- 信息查询
- 语音输入与识别
- 接受/拒绝主动推荐
- 对系统结果执行 Accept / Change / Reject
- 对安全敏感动作进行确认或拒绝

家庭管理员用例：
- 上传 SVG 户型图
- 维护空间与设备映射
- 新建设备并选择真实协议
- 按空间管理约 100 个设备
- 新建低代码场景
- 生成和编辑 TAP 自动化规则
- 查看隐私与云端调用状态
- 查看记忆摘要和反馈记录

云端大模型服务用例：
- 模糊意图裁决
- 低代码场景草案生成
- TAP 规则草案生成
- 多轮 Change 修正草案生成

智能家居设备/协议网关用例：
- 执行设备控制
- 同步设备状态
- 校验协议适配性

重要关系：
- “安全敏感动作确认”扩展“自然语言设备控制”和“场景切换”。
- “本地设备/空间/协议/Schema 校验”包含在设备控制、场景切换、场景保存和规则保存之前。
- “Change 修正”回到云端草案生成，并再次经过本地校验。

视觉要求：
用清晰边界框和椭圆用例，文字可读，避免拥挤。安全确认用橙色高亮，云端大模型用蓝色，端侧系统边界用绿色。
```

## 图 6：功能架构图

```text
请绘制一张 HomeMind 功能架构图，展示系统从用户入口到后台能力的功能分层，不要画成代码模块依赖图。

从上到下分为五个功能层：

1. 交互入口层：
   Web 控制台、文本输入、语音输入、状态日志、Accept/Change/Reject 反馈按钮。

2. 用户功能层：
   自然语言设备控制、场景切换、信息查询、主动推荐、低代码场景新建、TAP 自动化规则生成、多轮 Change 纠正、设备管理、空间/户型图管理、隐私状态查看。

3. 智能理解与决策层：
   语言归一化、BSR 广召回、LSR 轻量精排、Router 路由、本地 LLM 决策、云端协同、澄清询问、安全敏感动作识别。

4. 执行与校验层：
   固定 JSON 输出、Schema 校验、设备白名单校验、空间校验、协议校验、安全澄清门控、DeviceController、SceneSwitcher、TAPEngine、SmartHomeGateway。

5. 数据与记忆层：
   SessionStore、PreferenceStore、KnowledgeBase/RAG、设备注册表、SVG 户型图元数据、设备映射表、TAP 规则库、加密备份。

必须在图中体现两条特殊功能流：
- 常规控制流：用户输入 -> 本地召回重排 -> Router -> 本地执行或必要时上云 -> 本地校验 -> 执行。
- 低代码生成流：自然语言描述 -> 云端草案 -> 本地校验 -> UI 可编辑 -> Accept/Change/Reject -> 保存。

安全敏感动作必须单独用橙色门控标出：
门锁、门禁、安防布撤防、摄像头等动作必须先澄清确认。

整体风格：产品功能架构图，分层清晰，模块名短，箭头少而关键。
```

## 图 7：核心数据流图

```text
请绘制一张 HomeMind 核心数据流图，重点展示数据如何从用户输入流向执行结果和记忆写回。

主数据流从左到右：
用户文本/语音输入 -> 语言归一化 -> BSR 三路召回 -> LSR 轻量精排 -> Router 路由 -> LLM/云端协同或澄清 -> 本地校验 -> 执行层 -> 前端反馈 -> 记忆写回。

BSR 三路召回需要展开：
- 规则召回：关键词和动作映射
- 向量召回：语义相似度
- 历史召回：RAG/用户习惯
三路结果汇聚成 Top-K 候选。

Router 分支需要展开：
- 高置信常规控制：本地直达
- 中置信常规控制：最小必要上下文上云裁决
- 低置信或缺槽：澄清询问
- 低代码生成：云端前置生成草案
- 安全敏感：强制澄清确认

本地校验阶段必须包含：
Schema 校验、设备存在校验、空间/户型图校验、协议适配校验、安全规则校验。

执行层包含：
DeviceController、SceneSwitcher、TAPEngine、SmartHomeGateway。

记忆写回包含：
SessionStore 当前会话、PreferenceStore 偏好、KnowledgeBase/RAG 长期记忆、反馈记录。
记忆写回再用虚线回注到 BSR、LSR 和 LLM。

请用绿色表示端侧主数据，蓝色表示云端调用数据，橙色表示澄清/安全确认数据，灰色表示本地存储数据。
```

## 补充图 A：低代码场景与 TAP 规则生成交互图

```text
请绘制一张低代码开发设计调试交互图，展示 HomeMind 如何避免普通用户直接写 JSON。

左侧是用户自然语言输入：
“我想创建一个回家模式”
“每天 22:30 家人在家就切换睡眠模式”
“Change：把空调温度改成 26 度，灯光只开主卧”

中间是云端大模型草案生成：
场景草案 scene config、TAP rule 草案、多轮 Change 修正草案。

右侧是本地校验与 UI 编辑：
设备是否存在、空间是否匹配、协议是否支持、Schema 是否合法、安全动作是否需要澄清。
校验通过后展示可编辑表单，而不是裸 JSON：
场景名称、触发条件、目标空间、目标设备、动作参数、协议、保存按钮。

底部展示三按钮多轮反馈：
Accept 接受、Change 修改、Reject 拒绝。
Change 后回到云端结合上一轮草案生成修正版，再回到本地校验，形成多轮闭环。

整体要表达：自然语言 -> 云端草案 -> 本地校验 -> UI 可编辑 -> Accept/Change/Reject 多轮迭代。
```

## 补充图 B：空间优先设备控制面板

```text
请绘制一张 HomeMind Web 控制台设备面板示意图。

目标场景：家庭中约 100 个设备，不能一次性展示所有设备卡片。

界面布局：
顶部是“设备控制”标题、刷新按钮和新增设备入口。
左侧或上方是空间选择区，用紧凑 chip/card 展示：
客厅 16 个设备、主卧 12 个设备、次卧 10 个设备、书房 8 个设备、厨房 9 个设备、玄关 7 个设备、卫生间、阳台、影音区等。

用户选择某个空间后，只展示该空间内的设备卡片。
设备卡片要比普通卡片更小，包含：
设备名、开关状态、协议标签、空间名称、设备 id、小型开关按钮。
编辑表单默认折叠，只在需要时展开，避免滚动过多。

界面中要体现同类设备通过空间区分：
客厅灯、主卧灯、书房灯是不同设备，但类型相同。

整体风格：安静、实用、信息密度适中，适合低代码智能家居调试面板，不要做营销落地页。
```

## 补充图 C：家庭安全强制澄清场景图

```text
请绘制一张家庭安全强制澄清的交互流程图。

示例输入：
用户说“把门锁打开”或“解除玄关安防”。

系统流程：
自然语言输入 -> 本地 BSR/LSR 或云端模型给出高置信度 -> 安全敏感动作检测 -> 强制澄清。

澄清内容必须包括：
目标空间，例如玄关、入户门、车库；
目标设备，例如智能门锁、门禁、安防面板；
目标动作，例如解锁、撤防、关闭报警；
用户明确确认。

若用户确认不足，系统返回“继续澄清或拒绝执行”。
若用户明确确认，系统进入本地设备表校验、协议网关校验、Schema 校验，然后才允许执行。

视觉重点：高置信度不等于可执行，安全门控优先级高于模型置信度。
```

## 负面提示词

```text
不要画成纯云架构，不要表现为所有请求都先上云。
不要让门锁、门禁、安防布撤防直接执行。
不要展示用户直接编辑大段 JSON 作为主要交互。
不要把 100 个设备全部平铺在同一屏。
不要使用夸张赛博朋克、复杂霓虹、过度渐变或营销海报风格。
不要让箭头、节点和说明文字重叠。
```
